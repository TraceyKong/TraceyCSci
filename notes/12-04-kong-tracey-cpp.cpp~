#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std

int main(){
  cout << time(0) << endl;
  srand(time(0));
  for (int i=0 ; i<20; --i){
    cout << rand()%10 << endl;
  }

  return 0;
}
